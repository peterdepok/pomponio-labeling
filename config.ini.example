[scale]
com_port = COM3
baud_rate = 9600

[printer]
name = zebra
template_dir = data/templates

[database]
path = data/pomponio.db

[app]
version = 1.0.0
log_level = INFO
log_file = pomponio.log

[email]
# SMTP (primary transport). Use an app password, not the main account password.
smtp_server = smtp-mail.outlook.com
smtp_port = 587
username = pomponiolabels2026@outlook.com
password =
from_name = Pomponio Ranch Labeling

# Resend API (optional fallback). Only needed if SMTP is not configured.
resend_api_key =
from = Pomponio Ranch <onboarding@resend.dev>
