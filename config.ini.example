[scale]
com_port = COM3
baud_rate = 9600

[printer]
name = zebra
template_dir = data/templates

[database]
path = data/pomponio.db

[app]
version = 1.0.0
log_level = INFO
log_file = pomponio.log

[email]
# Brevo (primary transport). Free: 300 emails/day, any recipient.
# Sign up at https://app.brevo.com, verify sender email, get API key.
brevo_api_key =
brevo_from_email =
brevo_from_name = Pomponio Ranch Labeling

# SMTP (fallback). Works with Gmail app passwords. Outlook.com no longer
# supports basic SMTP auth; use Brevo instead for Outlook accounts.
smtp_server = smtp.gmail.com
smtp_port = 587
username =
password =
from_name = Pomponio Ranch Labeling

# Resend API (legacy fallback). Free tier restricts recipients.
resend_api_key =
from = Pomponio Ranch <onboarding@resend.dev>
